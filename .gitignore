# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# local vs code config
.vscode/

# exclude the executables
json-ld-publisher.exe
json-ld-publisher
